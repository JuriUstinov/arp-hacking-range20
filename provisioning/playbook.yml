- name: set up server
  hosts: server
  become: yes
  roles:
    - name: server
      flag: "{{ user_flag }}"
      flag_2: "{{ root_flag }}"

- hosts: client
  become: true
  roles:
    - client

- hosts: home
  become: true
  roles:
    - kali
